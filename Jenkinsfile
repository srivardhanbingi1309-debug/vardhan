pipeline{
      agent any
  stages {
        stage('Compile'){
                steps{
                        sh 'gcc Hello.c'
                }
        }
  stage('Run'){
      steps{
          sh 'gcc Hello.c'
      }
  }
}
}
